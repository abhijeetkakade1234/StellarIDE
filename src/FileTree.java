
import javax.swing.JFrame;
import javax.swing.JPanel;

public class FileTree {
    // This class is used to display the file tree in the IDE
    public static JPanel filetreePanel;
    public static JFrame fileTreeFrame;

    // Fucked up constructor for the side panel 
    public FileTree() {
        // filetreePanel = new JPanel();   
        // fileTreeFrame = new JFrame();
        // fileTreeFrame.setTitle("Side Panel Example");
        // fileTreeFrame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);

        // filetreePanel.setBackground(Color.GRAY);
        // filetreePanel.setPreferredSize(new Dimension(200, 0)); 


        // fileTreeFrame.pack();
        // fileTreeFrame.setVisible(true);
    }
    
}
